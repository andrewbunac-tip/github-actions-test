name: Shell Commands
on: [push]
jobs:
  Run-shell-Command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: eccho "Hello World"
      - name: Multi-line Script
        run: |
          node -v
          npm -v
