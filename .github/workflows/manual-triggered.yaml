name: Manually Triggered
on: 
    workflow_dispatch:
        inputs:
            string:
                description: A String Input
                type: string
                required: true
                default: "Deafult Val"

jobs:
    log-info:
        runs-on: ubuntu-latest
        steps:
            - run: echo $GITHUB_REF
            - run: echo $GITHUB_SHA