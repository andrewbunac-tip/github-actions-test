name: External Events
on:
    repository_dispatch:
        types: [echo-a-string]

jobs:
    echo-a-string:
        runs-on: ubuntu-latest
        steps:
            - name: Echo a string
              run: echo "Repository Dispatch event received!"
